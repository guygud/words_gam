# 🎮 Игры со словами

Коллекция игр со словами на JavaScript: головоломка с вращением строк/столбцов и тетрис со словом дня.

## 🎯 Игры

### 🧩 Головоломка со словами (`puzzle.html`)
Вращайте строки и столбцы на поле 5x5, чтобы собрать слова. Когда слово собрано, соответствующий ряд или столбец фиксируется и больше не участвует в игре.

**Целевые слова:** Столб, Лампа, Книга, Штука, Груша

**Управление:**
- Клик по кнопкам рядом со строками/столбцами для вращения
- Клавиатура: цифры 1-5 для выбора строки/столбца, затем Q/A (строки) или W/S (столбцы)

### 📦 Тетрис со словом дня (`tetris.html`)
Классический тетрис, где вместо фигур падают буквы из слова дня. Собирайте слова, чтобы очищать строки!

## 🚀 Запуск локально

### Вариант 1: Простой HTTP сервер (рекомендуется)

**macOS/Linux:**
```bash
chmod +x start-server.sh
./start-server.sh
```

**Windows:**
```bash
start-server.bat
```

Затем откройте в браузере: `http://localhost:8000`

### Вариант 2: Python HTTP сервер
```bash
python3 -m http.server 8000
```

### Вариант 3: Node.js (http-server)
```bash
npx http-server -p 8000
```

## 🌐 GitHub Pages

Проект готов для публикации на GitHub Pages!

### Настройка GitHub Pages:

1. Перейдите в настройки репозитория: `Settings` → `Pages`
2. В разделе "Source" выберите:
   - Branch: `main`
   - Folder: `/ (root)`
3. Нажмите `Save`
4. Через несколько минут сайт будет доступен по адресу:
   `https://guygud.github.io/words_gam/`

### Что уже настроено:

- ✅ Файл `.nojekyll` - отключает обработку Jekyll (нужно для ES6 модулей)
- ✅ Главная страница `index.html` - выбор игры
- ✅ Все файлы используют относительные пути

## 📁 Структура проекта

```
├── index.html          # Главная страница (выбор игры)
├── puzzle.html         # Головоломка со словами
├── tetris.html         # Тетрис со словом дня
├── css/
│   └── style.css       # Стили
├── js/
│   ├── main.js         # Тетрис: точка входа
│   ├── config.js       # Тетрис: конфигурация
│   ├── puzzle/
│   │   ├── main.js     # Головоломка: точка входа
│   │   └── ...
│   └── ...
└── .nojekyll           # Для GitHub Pages
```

## 🛠 Технологии

- Vanilla JavaScript (ES6+ модули)
- HTML5 Canvas
- CSS3

## 📝 Лицензия

MIT
