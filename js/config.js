// Конфигурация игры

export const CONFIG = {
    // Размеры игрового поля
    BOARD_WIDTH: 6,
    BOARD_HEIGHT: 6,
    
    // Слово дня
    WORD_OF_DAY: 'МЕТРОПОЛИС',
    
    // Минимальная длина слова для зачёта
    MIN_WORD_LENGTH: 3,
    
    // Настройки очков
    BASE_LINE_SCORE: 100,        // Базовые очки за сгоревшую линию
    WORD_BONUS: 50,              // Множитель за длину слова
    WORD_OF_DAY_BONUS: 1000,     // Бонус за точное совпадение со словом дня
    
    // Скорость игры
    DROP_INTERVAL: 1000,         // Интервал падения фигуры в миллисекундах
    FAST_DROP_INTERVAL: 50,      // Интервал при ускоренном падении
    
    // Размер клетки на canvas (в пикселях)
    CELL_SIZE: 30,
    
    // Стандартные формы тетромино (относительные координаты)
    TETROMINO_SHAPES: {
        // Одиночная буква
        SINGLE: [
            [[0, 0]]
        ],
        I: [
            [[0, 1], [1, 1], [2, 1], [3, 1]]
        ],
        O: [
            [[0, 0], [0, 1], [1, 0], [1, 1]]
        ],
        T: [
            [[1, 0], [0, 1], [1, 1], [2, 1]],
            [[1, 0], [1, 1], [2, 1], [1, 2]],
            [[0, 1], [1, 1], [2, 1], [1, 2]],
            [[1, 0], [0, 1], [1, 1], [1, 2]]
        ],
        S: [
            [[1, 0], [2, 0], [0, 1], [1, 1]],
            [[1, 0], [1, 1], [2, 1], [2, 2]]
        ],
        Z: [
            [[0, 0], [1, 0], [1, 1], [2, 1]],
            [[2, 0], [1, 1], [2, 1], [1, 2]]
        ],
        J: [
            [[0, 0], [0, 1], [1, 1], [2, 1]],
            [[1, 0], [2, 0], [1, 1], [1, 2]],
            [[0, 1], [1, 1], [2, 1], [2, 2]],
            [[1, 0], [1, 1], [0, 2], [1, 2]]
        ],
        L: [
            [[2, 0], [0, 1], [1, 1], [2, 1]],
            [[1, 0], [1, 1], [1, 2], [2, 2]],
            [[0, 1], [1, 1], [2, 1], [0, 2]],
            [[0, 0], [1, 0], [1, 1], [1, 2]]
        ]
    },
    
    // Получить уникальные буквы из слова дня
    getUniqueLetters() {
        return [...new Set(this.WORD_OF_DAY.split(''))];
    }
};
